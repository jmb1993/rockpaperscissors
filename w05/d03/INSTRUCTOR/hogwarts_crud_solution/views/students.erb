<h1>Here are the students</h1>
  <%#= link_to "Create New Student", new_student_path %>
<% @students.each do |student| %>
  <div>
    <a href="/students/<%= student["id"] %>"><%= student["fname"] + " " +student["lname"] %></a> -
    <a href="/houses/<%= student["house_id"] %>"><%= student["house_name"] %></a>
  </div>
<% end %>
